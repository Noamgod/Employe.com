import {createTheme} from '@mui/material';


export const Theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#ffffff', // New primary color
        },
        secondary: {
            main: '#546f8c', // New secondary color
        },
    },
});